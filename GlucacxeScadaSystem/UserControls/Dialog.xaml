<UserControl x:Class="GlucacxeScadaSystem.UserControls.Dialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:system="clr-namespace:System;assembly=System.Runtime"
             xmlns:materialDesign="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
             Width="400" MinHeight="180">

    <Border CornerRadius="12" Padding="20" Background="White"
            md:ElevationAssist.Elevation="Elevation2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="提示"
                       FontSize="18"
                       FontWeight="Bold"
                       Foreground="#333"
                       Margin="0 0 0 12" />

            <TextBlock x:Name="TextBlock"
                       Grid.Row="1"
                       TextWrapping="Wrap"
                       FontSize="15"
                       Foreground="#555"
                       TextAlignment="Left"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal"
                        HorizontalAlignment="Right" Margin="0 20 0 0"
                        x:Name="ButtonPanel">
                <Button Content="确定"
                        Margin="0 0 10 0"
                        Style="{DynamicResource MaterialDesignElevatedButton}"
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}">
                    <Button.CommandParameter>
                        <system:Boolean>True</system:Boolean>
                    </Button.CommandParameter>
                </Button>

                <Button Content="取消"
                        Style="{DynamicResource MaterialDesignTextButton}"
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}">
                    <Button.CommandParameter>
                        <system:Boolean>False</system:Boolean>
                    </Button.CommandParameter>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
